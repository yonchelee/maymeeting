# 5월 월례회 · 디자인 가이드

장미(로즈) 컨셉의 패션 에디토리얼 스타일 가이드입니다. `index.html` 단일 파일에 인라인으로 구현되어 있으며, 색상은 `:root`의 CSS 변수로 토큰화되어 있어 변수 값만 바꾸면 전체 톤이 한 번에 전환됩니다.

> 컨셉 한 줄: **"장미가 피어나는 5월" — 더스티 로즈 톤 위에 잡지 표지 같은 명조 타이포.**

---

## 1. 컬러 팔레트 (로즈 톤)

차분하고 따뜻한 더스티 로즈 / 로즈골드 계열. 모든 색은 `--sakura-*` 변수로 정의됩니다(변수명은 레거시이며, 실제 값은 로즈 톤입니다).

| 토큰 | HEX | 용도 |
|------|-----|------|
| `--sakura-50`  | `#FDF6F4` | 가장 옅은 블러시 배경 |
| `--sakura-100` | `#FAE9E6` | 섹션 배경 틴트, 보더 |
| `--sakura-200` | `#F2D0CC` | 외곽 프레임 라인, 옅은 보더 |
| `--sakura-300` | `#E2ABA8` | 장식 라인, 보조 강조 |
| `--sakura-400` | `#CE8A8C` | 중간 로즈 |
| `--sakura-500` | `#B76E79` | **메인 로즈**(킥커·강조·포인트) |
| `--sakura-600` | `#9A5560` | 딥 로즈(제목 그라데이션, 그림자) |
| `--sakura-700` | `#6E3A45` | 와인 모브(가장 진한 포인트, 꽃잎 잎맥) |

### 잉크(텍스트) · 중립
| 토큰 | HEX | 용도 |
|------|-----|------|
| `--ink-900` | `#2E1C22` | 본문/제목 기본 (웜 다크 모브) |
| `--ink-700` | `#5A4148` | 보조 텍스트 |
| `--ink-500` | `#8C757C` | 캡션/서브 텍스트 |
| `--ink-300` | `#CDC0C3` | 비활성/플레이스홀더 |
| `--paper`   | `#FFFFFF` | 기본 지면 |
| `--cream`   | `#FBF3F1` | 카드/인용 박스 배경 |

### 그림자
```
--shadow-sm: 0 4px 16px  rgba(154,85,96,.10);
--shadow-md: 0 14px 40px rgba(154,85,96,.14);
--shadow-lg: 0 26px 70px rgba(154,85,96,.18);
```
그림자 색은 딥 로즈(`--sakura-600` = rgb 154,85,96) 기반으로, 회색 대신 로즈빛이 은은하게 깔립니다.

### 블루 액센트 (예외)
패키지(`pkg-*`) 섹션은 정보 전달용으로 `--ocean-*`(블루) 액센트를 사용합니다. 로즈 톤의 보색 포인트로만 제한적으로 쓰며, 그 외 영역에는 사용하지 않습니다.

---

## 2. 타이포그래피

| 역할 | 폰트 | 스타일 |
|------|------|--------|
| 마스트헤드 (`MAY`) | Playfair Display | 900, `clamp(108px,23vw,330px)`, 로즈 그라데이션 |
| 제목 (h1·h2·`.serif`) | Playfair Display → Gowun Batang | 700~800, 고대비 명조 |
| 본문 | Gowun Batang (→ Pretendard 폴백) | 400, line-height 1.65 |
| 킥커/라벨 (`.eyebrow` 등) | Playfair Display *Italic* | 500, uppercase, letter-spacing .14em, `--sakura-600` |
| 강조어 (`.accent`) | 상속 | *italic* + `--sakura-600` |

- **로딩**: Google Fonts — `Playfair Display`(ital 포함), `Gowun Batang`, `Noto Serif KR`(폴백) + Pretendard(jsDelivr).
- 한글 제목은 Playfair에 글리프가 없어 자동으로 Gowun Batang으로 폴백됩니다(영문=Playfair, 한글=Gowun Batang).

---

## 3. 핵심 컴포넌트

- **외곽 프레임** `body::before` — 화면 가장자리에 `inset:clamp(10px,1.6vw,22px)`의 1px `--sakura-200` 라인. 잡지 지면 느낌의 시그니처.
- **킥커(eyebrow)** — 둥근 알약 칩이 아니라, 이탤릭 명조 텍스트 + 하단 1.5px `--sakura-300` 밑줄.
- **MAY 마스트헤드** — 인트로의 거대한 영문 + 그 아래 한글 부제(`.intro-title-ko`) + 마름모 장식 라인(`.intro-rule`).
- **사이드 내비게이션** — 우측 고정 `.nav-dot`(섹션당 1개, `data-target`로 매핑). 활성 시 `--sakura-500`로 채워지고 1.35배 확대.
- **프레임 카드** `.hobby-frame` — 라운드 14~20px, `--shadow-lg`. 미니게임 스크린샷/이미지 프레임에 재사용.
- **인용 박스** `.closing-quote` — `--cream` 배경, Playfair 이탤릭 큰따옴표 장식.

---

## 4. 장식 모티프 (장미)

- **떨어지는 장미 꽃잎** — `#petals-canvas`의 Canvas 애니메이션. 잎맥이 있는 넓은 장미 꽃잎 형태로, 느리고 무겁게 떨어집니다.
  - 꽃잎 색: `['#E2ABA8','#CE8A8C','#B76E79','#9A5560','#F2D0CC']`
  - 잎맥: `rgba(110,58,69,…)` (`--sakura-700`)
- **마무리 장미 한 송이** — closing 섹션의 인라인 SVG(겹꽃잎 + 진한 중심 + 줄기/잎). 색은 위 팔레트와 동일 계열.

---

## 5. 레이아웃 · 간격

- 섹션: `min-height:100dvh`, 패딩 `120px 48px 80px`, `scroll-snap-align:start`로 한 화면씩 스냅.
- 콘텐츠 폭: `.wrap` 최대 1200px.
- 라운드: 라인/칩 0, 카드 14~24px, 버튼/필 100px.
- 등장 애니메이션: `.reveal`(+`.d1~d4` 지연)로 스크롤 시 페이드업.

---

## 6. 톤 변경 방법

전체 톤을 바꾸려면 `index.html` `:root`의 `--sakura-50~700`, `--ink-*`, `--cream`, `--shadow-*` 값을 교체하면 됩니다. 추가로 변수를 우회해 하드코딩된 색이 다음 3곳에 있으니 함께 맞춰주세요.
1. 꽃잎 색 배열 `const COLORS=[…]` (Canvas 스크립트)
2. 꽃잎 잎맥 `rgba(110,58,69,…)`
3. 마무리 장미 SVG의 `fill="#…"` 값들

---

## 7. 사용 규칙

- 강조는 **로즈 1색 + 이탤릭**으로 절제 있게. 여러 색을 동시에 쓰지 않습니다.
- 제목은 명조(Playfair/Gowun), 마이크로 UI 라벨은 이탤릭 킥커로 위계를 만듭니다.
- 블루(`--ocean-*`)는 패키지 섹션의 정보 강조에만 사용합니다.
- 순백(`#FFF`)·순흑 대신 `--paper`/`--ink-900`(웜 톤)을 사용해 로즈 무드를 유지합니다.
